# Shura backend documentation

This placeholder file is retained only so old links do not fail. Current backend architecture, API boundaries, schema lifecycle, authentication rules, and local setup begin at [`../docs/README.md`](../docs/README.md). Do not reconstruct setup from legacy backend guides without checking current source and ordered migrations.
